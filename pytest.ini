[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    e2e: mark test as end-to-end (requires containers)
    integration: mark test as integration test
filterwarnings =
    ignore::DeprecationWarning
addopts = -v --tb=short
