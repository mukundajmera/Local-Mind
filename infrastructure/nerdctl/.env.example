# ============================================================
# Sovereign Cognitive Engine - Environment Configuration
# ============================================================
#
# SETUP:
#   1. Copy this file:  cp .env.example .env
#   2. Edit .env and set all REQUIRED values
#   3. Secure the file:  chmod 600 .env
#
# IMPORTANT: Never commit .env to version control!
# ============================================================

# -----------------------------------------------------------
# REQUIRED SECRETS (stack will fail to start without these)
# -----------------------------------------------------------

# Neo4j database password (minimum 8 characters)
NEO4J_PASSWORD=

# MinIO object storage secret key (minimum 8 characters)
MINIO_SECRET_KEY=

# -----------------------------------------------------------
# OPTIONAL - Authentication
# -----------------------------------------------------------

# Neo4j username (default: neo4j)
# NEO4J_USER=neo4j

# MinIO access key (default: minioadmin)
# MINIO_ACCESS_KEY=minioadmin

# -----------------------------------------------------------
# OPTIONAL - Neo4j Memory Tuning
# -----------------------------------------------------------

# Initial heap size (default: 512m)
# NEO4J_HEAP_INITIAL=512m

# Maximum heap size (default: 1G)
# NEO4J_HEAP_MAX=1G

# Page cache size (default: 512m)
# NEO4J_PAGECACHE=512m

# -----------------------------------------------------------
# OPTIONAL - Voice/TTS Service
# -----------------------------------------------------------

# TTS model to use (default: kokoro-82m)
# TTS_MODEL=kokoro-82m

# GPU device index (default: 0)
# CUDA_VISIBLE_DEVICES=0

# -----------------------------------------------------------
# OPTIONAL - Runtime Configuration
# -----------------------------------------------------------

# Environment mode: development, staging, production
# ENVIRONMENT=production

# Log verbosity: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Node.js environment for frontend
# NODE_ENV=production

# Frontend exposed port (default: 3000)
# INTERFACE_PORT=3000
